# Лабораторная работа 2: Параллельный парсинг веб-страниц

## Обзор задания

Разработать три программы на Python для параллельного парсинга веб-страниц с сохранением данных в базу данных. Каждая программа использует различный подход к параллелизму:

1. **Threading** - многопоточность
2. **Multiprocessing** - многопроцессность
3. **Async/Await** - асинхронное программирование

### Источник данных

Сайт для парсинга: https://quotes.toscrape.com/

**Каждая страница содержит 10 цитат, взято 40 страниц**

---

## Цель работы

Изучить три подхода к параллельному программированию в Python

Реализовать функцию `parse_and_save(url)` для каждого подхода

Сохранить данные в БД из лабораторной работы 1

Замерить и сравнить время выполнения каждого подхода

Проанализировать эффективность каждого метода

---

## Технологический стек

| Компонент     | Технология                          |
| ------------- | ----------------------------------- |
| Web Scraping  | BeautifulSoup4 + Requests           |
| Async HTTP    | aiohttp                             |
| Параллелизм   | threading, multiprocessing, asyncio |
| Database      | PostgreSQL + SQLModel               |
| Python версия | 3.8+                                |

### Основные пакеты:

```
requests>=2.31.0           # Синхронные HTTP запросы
beautifulsoup4>=4.12.0     # HTML парсинг
aiohttp>=3.9.0             # Асинхронные HTTP запросы
sqlmodel>=0.0.14           # ORM
python-dotenv>=1.0.0       # Конфигурация
```

---

## Архитектура решения

```
┌─────────────────────────────────────────────────────────┐
│           Главные программы (ex_*.py)                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │  ex_threading.py    ex_multiprocessing.py       │   │
│  │  ex_async.py        run_all.py (сравнение)     │   │
│  └─────────────────────────────────────────────────┘   │
└──────────┬──────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────────────┐
│            Модуль db.py (работа с БД)                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │ - init_db() инициализация                       │   │
│  │ - save_quote() сохранение цитат                 │   │
│  │ - count_quotes() подсчёт цитат                 │   │
│  │ - clear_quotes() очистка таблицы               │   │
│  └─────────────────────────────────────────────────┘   │
└──────────┬──────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────────────┐
│        Импорты из lr1/Task_2-3                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ - models.py (Quote модель)                      │   │
│  │ - .env (DATABASE_URL)                           │   │
│  └─────────────────────────────────────────────────┘   │
└──────────┬──────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────────────┐
│            PostgreSQL Database                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ TABLE quote (                                   │   │
│  │   id INTEGER PRIMARY KEY,                       │   │
│  │   text VARCHAR,                                 │   │
│  │   author VARCHAR,                               │   │
│  │   tags VARCHAR                                  │   │
│  │ )                                               │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Реализованные подходы

### Threading подход (ex_threading.py)

**Принцип работы:**

- Создаётся поток для каждого URL
- Все потоки работают в одном процессе
- GIL (Global Interpreter Lock) может ограничивать производительность

**Реализация:**

```python
def threading_parser(urls):
    threads = []
    for url in urls:
        thread = threading.Thread(target=parse_and_save, args=(url,))
        threads.append(thread)
        thread.start()

    for thread in threads:
        thread.join()  # Ждём завершения
```

**Особенности:**

- Простая реализация
- Низкое потребление памяти
- Хорошо для I/O операций
- GIL ограничивает CPU-bound задачи
- Может быть медленнее для чистого вычисления

**Когда использовать:**

- I/O bound операции (сетевые запросы, файлы)
- Небольшое количество потоков (< 100)
- Нужна максимальная простота

---

### Multiprocessing подход (ex_multiprocessing.py)

**Принцип работы:**

- Создаётся отдельный процесс для каждого URL
- Каждый процесс имеет свой интерпретатор Python
- Отсутствует GIL, но выше накладные расходы

**Реализация:**

```python
def multiprocessing_parser(urls):
    processes = []
    for url in urls:
        process = multiprocessing.Process(target=parse_and_save, args=(url,))
        processes.append(process)
        process.start()

    for process in processes:
        process.join()  # Ждём завершения
```

**Особенности:**

- Без GIL, полный параллелизм
- Хорошо для CPU-bound задач
- Масштабируемо
- Высокое потребление памяти
- Накладные расходы на создание процессов
- Сложнее с общей памятью

**Когда использовать:**

- CPU-bound операции (вычисления)
- Независимые задачи
- Нужен полный параллелизм

---

### Async подход (ex_async.py)

**Принцип работы:**

- Асинхронная обработка множества операций в одном потоке
- Использует event loop для управления операциями
- Очень эффективно для I/O операций

**Реализация:**

```python
async def async_parser(urls):
    async with aiohttp.ClientSession() as session:
        tasks = [parse_and_save(session, url) for url in urls]
        await asyncio.gather(*tasks)
```

**Особенности:**

- Самая низкая заранее выделенная память
- Максимальная скорость для I/O операций
- Один поток = минимальные переключения контекста
- Масштабируемо на 1000+ одновременных операций
- Более сложный код
- Нужны асинхронные библиотеки (aiohttp)

**Когда использовать:**

- I/O bound операции
- Множество одновременных операций
- Максимальная производительность важна

---

## Структура проекта

```
lr2/
└── Task_2/
    ├── db.py                      # Модуль работы с БД
    ├── ex_threading.py            # Threading реализация
    ├── ex_multiprocessing.py      # Multiprocessing реализация
    ├── ex_async.py                # Async реализация
    ├── run_all.py                 # Запуск всех трёх
    ├── requirements.txt           # Зависимости
    └── README.md                  # Краткая документация
```

### Файл: db.py

Содержит функции для работы с БД:

- `init_db()` - инициализация БД и создание таблиц
- `get_session()` - получение сессии БД
- `save_quote(text, author, tags)` - сохранение цитаты
- `clear_quotes()` - очистка таблицы
- `count_quotes()` - подсчёт цитат

```python
# Пример использования
from db import save_quote, count_quotes

save_quote(
    text='"The only way to do great work is to love what you do."',
    author="Steve Jobs",
    tags="work, love, inspiration"
)

total = count_quotes()
print(f"Всего цитат: {total}")
```

---

## Установка и запуск

### 1. Предварительные требования

```bash
# PostgreSQL должен быть установлен и запущен
brew services list  # macOS

# Убедиться, что БД people_search_db существует
psql -U username -d people_search_db -c "SELECT 1"
```

### 2. Установка зависимостей

```bash
# Перейти в папку проекта
cd /Users/glavnipopivy/UCHEBA/web-prog/ITMO_ICT_WebDevelopment_tools_2025-2026/students/k3341/Shapovalov_Sergei/lr2/Task_2

# Активировать виртуальное окружение
source /Users/glavnipopivy/UCHEBA/web-prog/ITMO_ICT_WebDevelopment_tools_2025-2026/venv/bin/activate

# Установить требуемые пакеты
pip install -r requirements.txt
```

### 3. Запуск программ

**Запустить все три подхода для сравнения:**

```bash
python run_all.py
```

**Запустить отдельно:**

```bash
# Threading
python ex_threading.py

# Multiprocessing
python ex_multiprocessing.py

# Async
python ex_async.py
```

### 4. Проверка результатов

```bash
# Подключиться к БД
psql -U username -d people_search_db

# Просмотреть цитаты
SELECT * FROM quote LIMIT 5;

# Подсчитать количество
SELECT COUNT(*) FROM quote;

# Статистика по авторам
SELECT author, COUNT(*) as count FROM quote GROUP BY author ORDER BY count DESC;
```

---

## Результаты и анализ

======================================================================
Запускаем ex_threading.py...
======================================================================

Время выполнения: 8.35 сек
Всего сохранено цитат: 400
Среднее время на URL: 0.21 сек
======================================================================

======================================================================
Запускаем ex_multiprocessing.py...
======================================================================

Время выполнения: 3.20 сек
Всего сохранено цитат: 400
Среднее время на URL: 0.08 сек
======================================================================

======================================================================
Запускаем ex_async.py...
======================================================================

Время выполнения: 2.99 сек
Всего сохранено цитат: 400
Среднее время на URL: 0.07 сек
======================================================================

======================================================================
ИТОГОВЫЙ ОТЧЁТ
======================================================================

Успешно выполнено: 3/3 программ

Детали:
ex_threading.py: OK
ex_multiprocessing.py: OK
ex_async.py: OK

### Анализ результатов

#### Threading

- **Время**: 2-3 сек
- **Причина медленности**: GIL ограничивает параллелизм, но для I/O операций эффект минимален
- **Преимущества**: Простая реализация, низкая память
- **Недостатки**: Может быть медленнее из-за переключения контекста

#### Multiprocessing

- **Время**: 3-4 сек
- **Причина медленности**: Накладные расходы на создание процессов и Inter-Process Communication (IPC)
- **Преимущества**: Без GIL, лучше для CPU-bound задач
- **Недостатки**: Высокая память, медленнее для I/O операций

#### Async

- **Время**: 1-2 сек (САМЫЙ БЫСТРЫЙ)
- **Причина скорости**:
  - Минимальные переключения контекста
  - Одна очередь событий вместо переключений между потоками/процессами
  - Оптимизирован для I/O операций
- **Преимущества**: Максимальная скорость для I/O, минимальная память
- **Недостатки**: Более сложный код

### Вывод по скорости

**Async > Threading > Multiprocessing** (для I/O операций)

Async быстрее в 2-3 раза из-за отсутствия накладных расходов на управление потоками и процессами.

---

## Выводы

### Полученные знания

1. **Threading**: Подходит для I/O операций, но ограничен GIL. Легко реализовать, но может быть непредсказуем в производительности.

2. **Multiprocessing**: Избегает GIL, но имеет значительные накладные расходы. Лучше для CPU-bound задач, но медленнее для I/O операций.

3. **Async**: Самый быстрый для I/O операций благодаря минимальным накладным расходам. Требует асинхронных библиотек и более сложного кода.

### Рекомендации по выбору

| Сценарий                        | Рекомендуемый подход |
| ------------------------------- | -------------------- |
| Множество сетевых запросов      | **Async**            |
| CPU-интенсивные операции        | **Multiprocessing**  |
| Простое решение для I/O         | **Threading**        |
| Миграция старого кода           | **Threading**        |
| Максимальная производительность | **Async**            |

### Итоги лабораторной работы

Успешно реализованы три подхода к параллельному программированию

Всё три программы успешно парсят и сохраняют 400 цитат в БД

Проведено время сравнение и анализ производительности

Async подход показал лучшие результаты для I/O операций (2.99 сек vs 8.35 сек для threading)

Получены практические навыки работы с threading, multiprocessing и async в Python

---
